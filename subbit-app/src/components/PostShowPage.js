import React, { Component } from 'react';
import Post from './Post';

export class PostShowPage extends Component {
    render() {
        return (
            <div>
                Hello post show page
            </div>
        )
    }
}

export default PostShowPage
